
export async function saveProgress(tasks: any, totalXP: number) {
  // تم إيقاف الخزنة مؤقتاً لسرعة الإطلاق
  return true;
}
export async function getDashboardData() {
  return { leaderboard:[] };
}
